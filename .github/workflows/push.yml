name: 'Release 🚀'

on:
  push:
    branches:
      - alpha
      - beta

  workflow_dispatch:
    inputs:
      reset-nx-cache:
        description: 'Reset Nx cache'
        required: false
        type: boolean
        default: false

jobs:
  test:
    uses: ./.github/workflows/--test.yml
    secrets: inherit
    with:
      reset-nx-cache: ${{ github.event.inputs.reset-nx-cache && true || false }}

  release:
    name: 'Publish new release 🚀'
    runs-on: ubuntu-22.04
    needs: test
    # TODO: remove the next line when updating to 1.0.0
    if: always()
    steps:
      - uses: actions/checkout@v3
        with:
          persist-credentials: false

      - uses: ./.github/actions/setup
        id: setup
        with:
          app-id: ${{ secrets.MYPARCEL_APP_ID }}
          private-key: ${{ secrets.MYPARCEL_APP_PRIVATE_KEY }}
          reset-nx-cache: ${{ github.event.inputs.reset-nx-cache }}

      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
          token: ${{ steps.setup.outputs.token }}

      - name: 'Run monodeploy'
        run: |
          yarn monodeploy \
            --config-file monodeploy.config.cjs \
            --log-level 0
        env:
          GH_TOKEN: ${{ steps.setup.outputs.token }}
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
