{
  "name": "@myparcel-vfb/monorepo",
  "version": "1.0.0-alpha.0",
  "private": true,
  "repository": "github:myparcelnl/vue-form-builder",
  "type": "module",
  "workspaces": [
    "packages/**"
  ],
  "scripts": {
    "build": "nx build",
    "build:dev": "nx build:dev",
    "buildOnly": "nx buildOnly",
    "clean": "nx run-many --target=clean --exclude=.",
    "prepare": "is-ci || husky install",
    "serve": "yarn build:dev && npx concurrently yarn:serve:demo yarn:serve:watch",
    "serve:demo": "nx serve demo",
    "serve:watch": "nx run-many --target=watch --exclude=.",
    "test": "vitest",
    "test:run": "vitest run --coverage",
    "typecheck": "nx run-many --target=typecheck --exclude=.",
    "workspace:clean": "cd $0 && rimraf lib",
    "workspace:tsup:build": "cd $0 && tsup src/index.ts --dts",
    "workspace:tsup:build:dev": "cd $0 && tsup src/index.ts --sourcemap --dts",
    "workspace:tsup:buildOnly": "cd $0 && tsup src/index.ts",
    "workspace:tsup:typecheck": "cd $0 && tsc src/index.ts --noEmit",
    "workspace:tsup:watch": "cd $0 && tsup src/index.ts --sourcemap --dts --watch",
    "workspace:vite:build": "cd $0 && vite build",
    "workspace:vite:build:dev": "cd $0 && vite build --mode development",
    "workspace:vite:buildOnly": "cd $0 && vite build",
    "workspace:vite:typecheck": "cd $0 && vue-tsc --noEmit",
    "workspace:vite:watch": "cd $0 && vite build --mode development --watch"
  },
  "lint-staged": {
    "*.{ts,vue}": "eslint --fix",
    "package.json": "npx sort-package-json"
  },
  "prettier": "@myparcel/prettier-config",
  "devDependencies": {
    "@monodeploy/plugin-github": "^0.6.2",
    "@myparcel-eslint/eslint-config-esnext": "^1.2.0",
    "@myparcel-eslint/eslint-config-node": "^1.0.0-alpha.2",
    "@myparcel-eslint/eslint-config-prettier": "^1.0.0-alpha.2",
    "@myparcel-eslint/eslint-config-prettier-typescript": "^1.0.0-alpha.1",
    "@myparcel-eslint/eslint-config-prettier-typescript-vue": "^1.0.0-alpha.1",
    "@tsconfig/recommended": "^1.0.1",
    "@vitest/coverage-c8": "^0.25.1",
    "@vue/test-utils": "^2.2.3",
    "@vue/tsconfig": "^0.1.3",
    "conventional-changelog-conventionalcommits": "^5.0.0",
    "eslint": "^8.27.0",
    "happy-dom": "^7.7.0",
    "husky": "^8.0.2",
    "is-ci": "^3.0.1",
    "jsdom": "^20.0.2",
    "lint-staged": "^13.0.3",
    "monodeploy": "^3.3.1",
    "nx": "^15.0.13",
    "prettier": "^2.7.1",
    "rimraf": "^3.0.2",
    "tsup": "^6.5.0",
    "typescript": "^4.8.4",
    "vitest": "^0.25.2",
    "vue": "^3.2.45"
  },
  "packageManager": "yarn@3.2.4"
}
