{
  "name": "@myparcel-vfb/monorepo",
  "version": "1.0.0-alpha.0",
  "private": true,
  "repository": "github:myparcelnl/vue-form-builder",
  "type": "module",
  "workspaces": [
    "libs/**",
    "apps/**"
  ],
  "scripts": {
    "build": "nx build --runner=cloud --output-style=stream",
    "build:demo": "nx build demo --runner=cloud --output-style=stream",
    "build:dev": "run build:dev:default --runner=cloud --output-style=stream",
    "build:dev:default": "nx build:dev --output-style=stream",
    "clean": "nx run-many --target=clean",
    "prepare": "is-ci || husky install",
    "serve": "run build:dev:default && npx concurrently yarn:serve:demo yarn:serve:watch",
    "serve:demo": "nx serve demo",
    "serve:watch": "run build:dev:default --output-style=stream && nx watch --all -d -- nx run \\$NX_PROJECT_NAME:build:dev --output-style=stream",
    "test": "nx run-many --target=test --parallel --output-style=stream",
    "test:run": "nx run-many --target=test:run --parallel=false --output-style=stream",
    "typecheck": "nx run-many --target=typecheck",
    "ws:clean": "cd $0 && rimraf dist",
    "ws:test": "cd $0 && vitest",
    "ws:test:run": "cd $0 && vitest run --coverage",
    "ws:tsup:build": "cd $0 && tsup-node src/index.ts --dts",
    "ws:tsup:build:dev": "cd $0 && NODE_ENV=development tsup-node src/index.ts --sourcemap --dts",
    "ws:tsup:typecheck": "cd $0 && tsc src/index.ts --noEmit",
    "ws:tsup:watch": "cd $0 && tsup-node src/index.ts --sourcemap --dts --watch",
    "ws:vite:build": "cd $0 && vite build",
    "ws:vite:build:dev": "cd $0 && vite build --mode development",
    "ws:vite:typecheck": "cd $0 && vue-tsc --noEmit --declarationMap false -p tsconfig.build.json",
    "ws:vite:watch": "cd $0 && vite build --mode development --watch"
  },
  "lint-staged": {
    "*.{json,md,html}": "prettier --write",
    "*.{ts,vue}": [
      "eslint --fix --cache",
      "nx affected --uncommitted --target=test:run --parallel=false --output-style=stream"
    ],
    "package.json": "npx sort-package-json"
  },
  "prettier": "@myparcel/prettier-config",
  "dependencies": {
    "@nrwl/nx-cloud": "^16.0.5"
  },
  "devDependencies": {
    "@monodeploy/plugin-github": "^1.0.1",
    "@myparcel-eslint/eslint-config-esnext": "^1.2.0",
    "@myparcel-eslint/eslint-config-node": "^1.2.0",
    "@myparcel-eslint/eslint-config-prettier": "^1.2.0",
    "@myparcel-eslint/eslint-config-prettier-typescript": "^1.2.0",
    "@myparcel-eslint/eslint-config-prettier-typescript-vue": "^1.2.0",
    "@nrwl/workspace": "^16.2.1",
    "@tsconfig/recommended": "^1.0.2",
    "@types/node": "^18.14.0",
    "@vue/tsconfig": "^0.4.0",
    "bundlewatch": "^0.3.3",
    "conventional-changelog-conventionalcommits": "^5.0.0",
    "eslint": "^8.34.0",
    "eslint-plugin-sort-exports": "^0.8.0",
    "husky": "^8.0.3",
    "is-ci": "^3.0.1",
    "lint-staged": "^13.1.2",
    "monodeploy": "^4.0.1",
    "nx": "^16.2.1",
    "prettier": "^2.8.4",
    "rimraf": "^5.0.1",
    "typescript": "^5.0.0"
  },
  "packageManager": "yarn@3.5.1",
  "engines": {
    "node": ">=18.0.0"
  },
  "volta": {
    "node": "16.19.1"
  },
  "bundlewatch": {
    "ci": {
      "trackBranches": [
        "main",
        "alpha",
        "beta"
      ]
    },
    "files": [
      {
        "path": "./apps/vue-form-builder/dist/**/*.js"
      },
      {
        "path": "./libs/core/dist/**/*.js"
      },
      {
        "path": "./libs/hook-manager/dist/**/*.js"
      },
      {
        "path": "./libs/plugin/dist/**/*.js"
      },
      {
        "path": "./libs/utils/dist/**/*.js"
      }
    ]
  }
}
